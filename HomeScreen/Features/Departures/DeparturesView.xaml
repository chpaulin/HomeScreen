<UserControl
    x:Class="HomeScreen.Features.Departures.DeparturesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HomeScreen.Features.Departures"    
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"    
    mc:Ignorable="d"
    d:DesignHeight="740"
    d:DesignWidth="525"
    RequestedTheme="Dark">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" FontFamily="ms-appx:/Assets/GlyphyxOneNF.otf#Glyphyx One NF" Text="&amp;" Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}" FontSize="70" HorizontalAlignment="Center" VerticalAlignment="Bottom" />
        </Grid>
        <ItemsControl Grid.Column="0" Grid.Row="1" ItemsSource="{Binding Departures}" Margin="0,20,0,0" >
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <local:DepartureView />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
