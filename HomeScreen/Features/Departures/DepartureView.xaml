<UserControl
    x:Class="HomeScreen.Features.Departures.DepartureView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HomeScreen.Features.Departures"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:c="using:HomeScreen.Common.Converters"
    mc:Ignorable="d"
    d:DesignHeight="75"
    d:DesignWidth="250" RequestedTheme="Dark">
    <UserControl.Resources>
        <c:DateTimeToDepartureConverter x:Key="DateTimeToDepartureConverter"/>
    </UserControl.Resources>

    <Grid Margin="0,5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Rectangle Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" Fill="{StaticResource SystemControlHighlightAccentBrush}"/>
        <TextBlock Grid.Column="1" Text="{Binding Departs, Converter={StaticResource DateTimeToDepartureConverter}, FallbackValue=5 min}" FontSize="42" Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}" HorizontalAlignment="Left" Margin="10,0,0,0"/>
    </Grid>
</UserControl>
